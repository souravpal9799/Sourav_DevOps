apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://{{ hostvars['web'].private_ip_addr }}:{{ prometheus_port }}
    isDefault: true
    editable: false

